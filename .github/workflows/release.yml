name: Release Murmure
on:
  workflow_dispatch:

jobs:
  linux:
    uses: ./.github/workflows/build-linux.yml
    secrets: inherit

  windows:
    uses: ./.github/workflows/build-windows.yml
    secrets: inherit
